# ADR-001: Graph-first Architecture

## Status

Proposed

## Context

rig transforms repositories into queryable engineering graphs. The core
data model choice — graph vs. relational vs. document — shapes every
downstream component (parsers, storage, query interface).

## Decision

_To be written: why a graph model (nodes/edges) is the right
representation for code entities and their relationships, over
alternatives like a relational schema or flat indices._

## Consequences

_To be written._
