# ADR-004: Storage Backend

## Status

Proposed

## Context

The engineering graph needs to be persisted and queried efficiently at
repository scale, with support for incremental updates as code changes.

## Decision

_To be written: choice of storage engine (embedded graph DB, SQLite with
a graph schema, external graph DB, etc.) and the tradeoffs that drove it._

## Consequences

_To be written._
